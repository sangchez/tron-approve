# Cloudflare Workers 静态资源部署（Vue SPA）
# 部署前须先执行: yarn build
# 部署命令: yarn deploy

name = "jolly-cake-bf27"
compatibility_date = "2025-06-26"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
